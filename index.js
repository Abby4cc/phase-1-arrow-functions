//  Function expression to return a number divided by 100
const divide = function(num) {
    return num / 100;
};

//  Arrow function to square a number
const square = num => num * num;

//  Arrow function to add two numbers
const add = (num1, num2) => num1 + num2;
